<?xml version="1.0" encoding="utf-8"?>
<com.google.android.material.card.MaterialCardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:id="@+id/card_shortcut"
    style="@style/Widget.AnimalLoo.Card"
    android:layout_width="0dp"
    android:layout_height="wrap_content"
    android:layout_columnWeight="1"
    android:layout_margin="@dimen/spacing_xs"
    android:clickable="true"
    android:focusable="true"
    app:cardBackgroundColor="@color/color_surface"
    app:rippleColor="@color/color_primary_light">

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:orientation="vertical"
        android:padding="@dimen/spacing_md">

        <FrameLayout
            android:layout_width="@dimen/shortcut_icon_container"
            android:layout_height="@dimen/shortcut_icon_container"
            android:background="@drawable/bg_shortcut_icon">

            <ImageView
                android:id="@+id/iv_shortcut_icon"
                android:layout_width="@dimen/icon_size_md"
                android:layout_height="@dimen/icon_size_md"
                android:layout_gravity="center"
                android:contentDescription="@null"
                app:tint="@color/color_primary" />

        </FrameLayout>

        <TextView
            android:id="@+id/tv_shortcut_label"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="@dimen/spacing_sm"
            android:gravity="center"
            android:textAppearance="@style/TextAppearance.AnimalLoo.Caption"
            android:textColor="@color/color_text_primary" />

    </LinearLayout>

</com.google.android.material.card.MaterialCardView>
